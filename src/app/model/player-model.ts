export class PlayerCreator {
  constructor(public firstName: string, public lastName: string, public score: number, public scoreArray: Array<number>){}
}
//May not need scoreArray if I can't figure out how to push values.
